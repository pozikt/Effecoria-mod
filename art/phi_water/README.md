# Phi-water art

`phi_water_bucket.png` — vanilla `water_bucket` recolored to aquamarine Φ-hydrolat.
Keep metal bucket pixels; only remap the blue fill.

Rule: prefer recoloring vanilla item/block textures when a close counterpart exists.
